所有文档，包括日志和项目说明内容，都要书面语，正式语气，适量引用代码，但是禁止以下类型的语言：中文：把代码特定用语和容易用1-2词解释的英文混合到中文里；英文：过度简略，过度抽象，代码特定用词和一般自然语言混杂

如果程序出现较大改版，不要保留原来路径，不要保留旧功能，一定要清理干净

如果涉及到python文件里面配置的具体数值，如果方便，都集中在同一个配置文件里面设

1. 坚持“一天只解决一个问题”。
第一步：Bootstrap 不再死循环（✔）
* Passage Mode 能正常进入和退出（待验证）
* Planner 能正常选中大角度出口（待验证）
* 开阔区不再长时间原地 align（待验证）
第二步：如果这些验证后都正常，而机器人仍然因为看不到低矮障碍、贴墙推物块导致失败，那么这就是深度相机该介入的时候。    不是用深度去修 Planner。
* 而是用深度补齐 LiDAR 的物理盲区。
2. 深度仅做安全增强，不参与规划决策。 保持其只补充 center/left/right 距离，影响 clearance_profile 与挡障，不参与目标评分、Recovery 或状态机决策，确保架构稳定。
3. 增加回退机制。 fusion_enabled 默认关闭，并保留一键切换到纯雷达模式，验收前优先选择实测更稳定的方案。

小车有前后两个方向，而相机只能看到一个方向。只能把相机作为前向信息引导