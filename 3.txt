我认真看了你整理的几版日志，我现在的判断和前面有一点不同。

我不建议继续修 CorridorCommit，也不建议继续调各种激光阈值了。

因为日志已经把问题收敛得很明确。

我认为真正的问题已经只有一个

现在系统已经证明：

* ✅ Frontier 能规划（nearest_frontier() 正常）
* ✅ A* 能生成 path（path=4、path=6）
* ✅ Navigator 能跟路径（wp 从 3→6）
* ✅ Channel auto-arm 已经退出历史舞台

说明以前怀疑的那些问题都不是现在的主因了。

真正失败的位置已经固定：

正常导航
    ↓
撞黄块 / narrow_passage
    ↓
Recovery
    ↓
arm_passage_mode(gap)
    ↓
Recovery结束
    ↓
Frontier goal 丢失
    ↓
MOVE_TO_GOAL(has_goal=False)
    ↓
不停 replan

注意这里最关键的一句：

Recovery 后不是”规划失败”，而是”规划结果丢了”。

这是两个完全不同的问题。

⸻

我甚至怀疑只有一两个地方有 Bug

根据你所有日志，我现在最怀疑的是下面几种情况。

第一种（概率最高）

Recovery 结束时执行了

frontier_nav.reset();

或者

clear_path();

或者

goal_.reset();

但是 FSM 又没有重新

nearest_frontier()
↓
set_path()

所以

has_goal=False

一直保持。

⸻

第二种

Recovery结束以后

FSM直接

RECOVERY
↓
MOVE_TO_GOAL

而不是

RECOVERY
↓
PLAN_NEXT
↓
重新规划

于是

MOVE_TO_GOAL

里面发现

has_goal=False

只能疯狂打印

no frontier goal

⸻

第三种

arm_passage_mode(gap) 把 Navigator 内部状态覆盖掉了。

例如

current_path_
↓
nullptr

或者

goal_idx_
↓
0

恢复时没有恢复。

这种情况我也见过。

⸻

我不会继续改算法

如果我是现在接手代码的人。

我今天下午不会再碰：

* CorridorCommit
* LocalPlanner
* OccupancyGrid
* A*
* 激光阈值
* Recovery动作

全部冻结。

⸻

我只查状态机

直接画状态图。

例如：

PLAN_NEXT
↓
MOVE
↓
RECOVERY
↓
？？？

Recovery结束以后到底去了哪里。

一定要把这条链路画出来。

⸻

然后全工程搜索

建议直接 grep：

has_goal
goal_
clear_goal
reset_goal
clear_path
set_path
goal_idx
current_path
passage_mode
arm_passage_mode

重点不是谁生成 path。

而是谁把 path 清掉。

⸻

我为什么不建议回滚

因为现在其实已经进步很多。

你自己整理的数据已经说明：

第一次

wp
3/6
↓
6/6

第二次

frontier reached
↓
重新规划
↓
wp
0/6
↓
4/6

这说明：

整个 Frontend→A*→Navigator 是活的。

这是之前根本没有做到的。

真正的问题只发生在

Recovery

以后。

所以如果现在回滚。

你会重新得到：

* Bootstrap抢控制
* Perimeter
* Channel auto arm

这些更大的问题。

⸻

我建议现在最后的策略

不要再做”功能修改”。

改成：

只修一个 Bug：Recovery 后为什么丢 Goal。

甚至可以把今天剩下的工作全部限定为：

保证 Recovery 结束一定回到 PLAN_NEXT，重新调用一次 nearest_frontier()+set_path()，而不是直接回 MOVE_TO_GOAL。

如果这个修不好，我宁可保持现在的版本，也不会再继续改避障逻辑。

⸻

我最后还有一句判断。

从你贴出来的所有日志来看，我认为你们已经不是算法设计失败，而是状态机生命周期管理出了问题。这类 Bug 往往不是几十个参数的问题，而是一个状态没有恢复、一个变量被清空、或者一个错误的状态跳转。它通常能通过很小的代码改动解决，但前提是不要再同时修改其他模块。现在最值得投入精力的，就是把 Recovery 前后相关的几十行代码彻底梳理一遍，而不是继续调几何阈值。